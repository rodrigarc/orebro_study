# Fix package-path to environment first
.libPaths(c(.libPaths()[2], .libPaths()[1]))


# If your using Linux or it doesn't work try the following instead:
# env_index <- grep("/envs/", .libPaths())
# .libPaths(c(.libpaths()[env_index],.libPaths()))
